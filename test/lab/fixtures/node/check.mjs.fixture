import assert from "node:assert/strict";
import { add } from "clean-lab-math";

assert.equal(add(5, 7), 12);
assert.equal(add(0, 0), 0);
assert.equal(add(-5, 7), 2);
console.log("3 arithmetic cases passed");
