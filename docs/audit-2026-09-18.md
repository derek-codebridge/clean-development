# Master-plan audit

Audit date: 18 September 2026. This records the repository state after the independent plan, integration, and security/release reviews.

## Local evidence

All of these checks passed from the repository root:

- `npm run check`
- `npm test` — 80/80 tests
- `npm run test:package` — 55-file tarball, empty-prefix install, executable and export checks
- `npm run smoke:tools` — Cargo, Go, npm, and uv
- `npm audit --omit=dev --audit-level=low` — 0 vulnerabilities
- all 14 declared adapter mappings — automated contract coverage
- Codex Plugin Creator validator
- Skill Creator validator
- Isolated Codex CLI 0.154.0 local marketplace add/install/list
- Isolated Grok Build 1.0.34 marketplace add/install/list/details with zero skills, commands, and agents

The latest four 100-iteration macOS arm64 runs measured 56.72–59.33 ms added median and 75.96–97.73 ms p95. Earlier runs reached 145.14 ms p95, so the provisional 75 ms p95 gate is not consistently met.

## Fix made during the audit

An explicit `setup --agents` selection now deactivates previously owned native Claude, Codex, and Grok integrations omitted from the new selection. It preserves unrelated configuration and leaves shared launchers available. A regression test covers the transition from all three native routes to Claude only.

The Codex repo marketplace also now uses the current local `source` / `path` schema, and `scripts/check.mjs` rejects a regression to the old form.

## Remaining release gates

These are not silently marked complete:

- real Claude, Codex desktop, Grok model, OpenCode, and Pi workflow acceptance, including restart/resume, child processes, cwd changes, and sandbox behavior;
- baseline/enabled model-facing request captures for token-neutrality evidence;
- Linux, Windows, native Apple/Xcode/SwiftPM, external-volume, full-disk, hostile-race, and real concurrent-worktree acceptance;
- native cache eviction and scratch expiry, which remain intentionally deferred until safe tool-specific concurrency contracts exist;
- npm/GitHub publication, provenance, repository settings, and public release checks.

The local Git repository is committed at the release-candidate commit shown by `git log`. Codex plugin validation requires a `defaultPrompt` UX field; it is explicitly worded and is not a startup/bootstrap instruction. Normal runtime activation remains context-free. No remote, npm publication, or home-directory cleanup was performed.
